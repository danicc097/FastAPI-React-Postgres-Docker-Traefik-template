#!/bin/bash

cd e2e || exit

n_runs=$1
test_args=("${@:2}")
rm ../benchmark-e2e-jest.log

for ((i = 0; i < n_runs; i++)); do
  ../bin/initial-data e2e
  yarn test-local:headless "${test_args[@]}" 2>&1 | tee -a ../benchmark-e2e-jest.log
done
