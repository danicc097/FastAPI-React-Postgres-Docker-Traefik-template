#!/bin/bash

#
# Run backend unit tests and frontend unit/integration tests in parallel
#

set -e

source "${BASH_SOURCE%/*}/.helpers.sh"

backend/scripts/run-tests.sh -n 4 --dist loadscope || exit 1 &
cd frontend || exit && yarn test:nowatch || exit 1 &

wait
